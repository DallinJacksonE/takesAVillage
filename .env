# Required shared secret for backend-to-bot authentication.
# Generate one for local/deployed use, for example:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(48))'
BOT_SECRET=replace-with-a-long-random-value
